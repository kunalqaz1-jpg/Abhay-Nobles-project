{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/school-erp run build && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "^/api/(.*)$", "dest": "/api/index" },
    { "src": "^(.*)$", "dest": "/index.html" }
  ]
}
